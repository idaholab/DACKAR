# Test-only dependencies for DACKAR (in addition to the package's runtime deps).
# Install into the same Python env you use for DACKAR:
#   pip install -r requirements-test.txt
# Then run the suite from the repo root:
#   pytest tests/                     # full suite
#   pytest tests/ -m "not slow"       # skip the Hypothesis property-based tier
pytest>=7.0
hypothesis>=6.0
