This folder contains Draft 7 JSON Schemas used by RCAArtifactValidator.

Filename convention:
  <artifact_name>.json

Current core orchestrator artifacts:
  event
  telemetry_summary
  kg_context
  causality_candidates
  evidence_bundle
  rca_card
  operational_context
  pm_compliance

Secondary / ingestion-side artifacts:
  document
  processed_text_record