# Epistemic calibration profile — Phase C
# Generated by: calibration procedure (§11.3 of epistemics_notes_4.md)
# Status: placeholder — values to be derived from TC-1–TC-7 runs with Phase C active
#
# Procedure:
#   1. Automate TC-1–TC-7 as integration tests
#   2. Run TC-1–TC-7 with Phase C routing active and minimum_evidence_threshold = 0.0
#   3. Record composite score distributions per test case
#   4. Set minimum_evidence_threshold to value preserving correct candidate selection
#   5. Establish confidence_override thresholds from same runs
#   6. Bump profile_version and store alongside EpistemicsRoutingConfig

profile_name: "phase_c_baseline"
profile_version: "TBD"
policy_version: "epistemics-v1.0"
baseline_data_coverage_signature: "TBD"

derived_thresholds:
  minimum_evidence_threshold: 0.35        # current default — recalibrate after TC-1–TC-7
  confidence_override_threshold_temporal: "TBD"
  confidence_override_threshold_telemetry: "TBD"

notes: >
  Recalibration required before Phase C is enabled in production.
  Current values are pre-Phase-C defaults and will not correctly gate
  candidates after the support_score routing restriction is active.
