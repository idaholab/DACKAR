{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScheduleImpactAssessment",
  "description": "Stage D output. Critical path and float analysis for inserting the emergent activity into the current outage schedule. Answers the question an outage manager actually manages: how much float does this consume, is it on the critical path, and what does it displace? Built by connecting the EmergentWorkWorkflow to the existing CriticalPathRiskAnalyzer and ScheduleGraph.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "activity_id",
    "run_id",
    "generated_at",
    "schedule_version_id",
    "duration_estimate",
    "float_analysis",
    "cp_impact",
    "confidence",
    "provenance"
  ],

  "properties": {

    "activity_id": { "type": "string" },
    "run_id": { "type": "string" },
    "generated_at": { "type": "string", "format": "date-time" },

    "schedule_version_id": {
      "type": "string",
      "description": "Identifier of the schedule version against which impact was computed. Must match OutageDataset.schedule_versions."
    },

    "insertion_point": {
      "type": "object",
      "additionalProperties": false,
      "description": "Where in the schedule network the emergent activity would be inserted for this assessment.",
      "properties": {
        "after_task_id": {
          "type": ["string", "null"],
          "description": "Task after which the emergent activity would start."
        },
        "before_task_id": {
          "type": ["string", "null"],
          "description": "Task that would be delayed by this insertion."
        },
        "outage_phase": {
          "type": ["string", "null"],
          "enum": ["cooldown", "defueling", "refueling", "maintenance", "startup", "power_ascension", "other", null]
        },
        "proposed_start": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "proposed_finish": {
          "type": ["string", "null"],
          "format": "date-time"
        }
      }
    },

    "duration_estimate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["p50_hours", "p80_hours", "p90_hours", "confidence_tier"],
      "description": "Probabilistic duration estimate derived from historical analogs (Stage E feeds back to refine this) or the fallback population.",
      "properties": {
        "p50_hours": { "type": "number", "minimum": 0 },
        "p80_hours": { "type": "number", "minimum": 0 },
        "p90_hours": { "type": "number", "minimum": 0 },
        "mean_hours": { "type": ["number", "null"], "minimum": 0 },
        "std_hours": { "type": ["number", "null"], "minimum": 0 },
        "distribution_type": {
          "type": ["string", "null"],
          "enum": ["lognormal", "normal", "empirical", "triangular", "fallback_population", null]
        },
        "confidence_tier": {
          "type": "string",
          "enum": ["data_supported", "sme_informed", "low_confidence"],
          "description": "data_supported: based on ≥5 historical analogs with good similarity. sme_informed: based on fewer analogs or SME-adjusted estimate. low_confidence: fallback population or no analog data available."
        },
        "sample_size": {
          "type": ["integer", "null"],
          "minimum": 0,
          "description": "Number of historical analogs used to fit the distribution."
        }
      }
    },

    "float_analysis": {
      "type": "object",
      "additionalProperties": false,
      "required": ["float_consumed_hours", "is_critical_path_impact", "criticality_label"],
      "properties": {
        "float_consumed_hours": {
          "type": "number",
          "description": "Total float consumed by inserting this activity at the assessed insertion point using the p50 duration estimate."
        },
        "available_float_before_hours": {
          "type": ["number", "null"],
          "description": "Total float available on the affected path before insertion."
        },
        "remaining_float_after_hours": {
          "type": ["number", "null"],
          "description": "Remaining float after insertion. Negative values indicate a new critical path."
        },
        "is_critical_path_impact": {
          "type": "boolean",
          "description": "True if inserting this activity at p50 duration would consume all available float and extend the critical path."
        },
        "criticality_label": {
          "type": "string",
          "enum": ["critical", "near_critical", "non_critical"],
          "description": "critical: activity is on or will force the critical path. near_critical: less than 8 hours of float remaining after insertion. non_critical: sufficient float available."
        }
      }
    },

    "cp_impact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["baseline_cp_hours", "estimated_new_cp_hours", "cp_drag_hours"],
      "description": "Critical path impact metrics. Produced by CriticalPathRiskAnalyzer.",
      "properties": {
        "baseline_cp_hours": {
          "type": "number",
          "minimum": 0,
          "description": "Baseline critical path duration for this schedule version."
        },
        "estimated_new_cp_hours": {
          "type": "number",
          "minimum": 0,
          "description": "Estimated new critical path duration after inserting the emergent activity at p50 duration."
        },
        "cp_drag_hours": {
          "type": "number",
          "description": "Expected increase in project duration attributable to this insertion. 0 if non-critical."
        },
        "cp_sensitivity_score": {
          "type": ["number", "null"],
          "minimum": 0,
          "maximum": 1,
          "description": "Point-biserial correlation between this activity's criticality status and project finish time, from Monte Carlo simulation. Values near 1.0 indicate this activity most reliably predicts delays when critical."
        },
        "p80_cp_hours": {
          "type": ["number", "null"],
          "description": "P80 critical path duration from Monte Carlo simulation using the duration distribution."
        },
        "p90_cp_hours": {
          "type": ["number", "null"],
          "description": "P90 critical path duration from Monte Carlo simulation."
        }
      }
    },

    "displaced_tasks": {
      "type": "array",
      "description": "Tasks that would be delayed as a result of inserting this emergent activity.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["task_id", "delay_hours", "has_regulatory_constraint"],
        "properties": {
          "task_id": { "type": "string" },
          "task_name": { "type": ["string", "null"] },
          "original_start": { "type": ["string", "null"], "format": "date-time" },
          "new_start": { "type": ["string", "null"], "format": "date-time" },
          "delay_hours": { "type": "number", "minimum": 0 },
          "has_regulatory_constraint": {
            "type": "boolean",
            "description": "True if the displaced task has a TS, NRC, or CAP constraint that limits how long it can be deferred."
          },
          "regulatory_constraint_note": { "type": ["string", "null"] }
        }
      }
    },

    "resource_conflicts": {
      "type": "array",
      "description": "Resource conflicts identified at the proposed insertion point.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["resource_id", "conflict_type"],
        "properties": {
          "resource_id": { "type": "string" },
          "resource_type": {
            "type": "string",
            "enum": ["crew", "vendor", "craft", "specialized_equipment"]
          },
          "conflict_type": {
            "type": "string",
            "enum": ["unavailable", "overloaded", "certification_required", "fatigue_limit", "other"]
          },
          "description": { "type": ["string", "null"] }
        }
      }
    },

    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Overall confidence in this schedule impact assessment, accounting for duration estimate uncertainty and schedule data completeness."
    },

    "notes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Analyst-visible notes about assumptions, limitations, or caveats for this assessment."
    },

    "provenance": {
      "type": "object",
      "additionalProperties": true,
      "required": ["generated_by", "run_id"],
      "properties": {
        "generated_by": { "type": "string" },
        "run_id": { "type": "string" },
        "schedule_graph_version": { "type": ["string", "null"] },
        "monte_carlo_runs": { "type": ["integer", "null"] }
      }
    }
  }
}
